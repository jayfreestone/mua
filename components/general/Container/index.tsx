import styled from 'styled-components';

/**
 * A wrapper component that centers and adds a max-width.
 */
const Container = styled.div`
  max-width: 1300px;
  margin: 0 auto;
`;

export default Container;
